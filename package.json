{
  "name": "copy-markdown-link",
  "version": "0.1.0",
  "description": "Copy the Markdown link of current page.",
  "main": "background.js",
  "scripts": {
    "lint": "dprint check && eslint src eslint.config.* && web-ext lint",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && web-ext build --overwrite-dest"
  },
  "author": "ile-24556",
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/firefox-webext-browser": "^143.0.0",
    "dprint": "^0.54.0",
    "eslint": "^10.3.0",
    "globals": "^17.6.0",
    "jiti": "^2.6.1",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.59.1",
    "web-ext": "^10.1.0"
  }
}
